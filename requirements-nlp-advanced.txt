# Python for Machine Learning - NLP / advanced-topics layer
# University of Idaho CS 4771/5771 (Python 3.11+)
#
# PyTorch layer plus everything imported by the NLP, transformers, generative,
# LLM, and MLOps modules (HW4 extensions, advanced-topic readings).
# Install: pip install -r requirements-nlp-advanced.txt
#
# TODO: compile to platform-specific hash-checked lock files (see requirements.txt).

-r requirements-pytorch.txt

# Transformers and datasets
transformers>=4.46,<6
datasets>=3,<6
accelerate>=1,<2
peft>=0.13,<1
diffusers>=0.31,<1
sentence-transformers>=3,<6

# Classical NLP and embeddings
nltk>=3.9,<4
gensim>=4.3,<5

# MLOps and serving
mlflow>=3.7,<4
fastapi>=0.115,<1
uvicorn>=0.30,<1
joblib>=1.4,<2
pydantic>=2.7,<3
httpx>=0.27,<1

# Explainability
shap>=0.46,<1
lime>=0.2,<1